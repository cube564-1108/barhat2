# HTTP client для Pyrus API
requests==2.32.3

# Бандл корневых сертификатов. Тянется как зависимость requests, но мы
# импортируем его напрямую в src/russian_ca.py — фиксируем явно.
certifi>=2024.7.4

# HTTP server (для JSON endpoint)
flask==3.0.3

# gzip-сжатие ответов: статика дашборда (~450 КБ текста) уходила несжатой
flask-compress==1.15

# Environment variables
python-dotenv==1.0.1

# WSGI server for production
gunicorn==23.0.0

# Authentication and session management
flask-login==0.6.3
flask-cors==4.0.1
pyjwt==2.9.0
