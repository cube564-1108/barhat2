meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  command: gunicorn app:app --bind 0.0.0.0:80 --workers 2 --timeout 120
  containerPort: 80
