# Автоматизация согласования и оплаты счетов — план

**Статус:** [ ] В процессе
**Дата:** 2026-08-14 (обновлено)
**Цель:** Свой модуль в дашборде (без Pyrus) для полного цикла счёта. Ручными остаются только: создание счёта, согласование (решение) и подпись/отправка платежа в банке. Всё остальное — автоматически.

---

## Контекст (as-is, до изменений)

1. Сотрудник заводит счёт в форме Pyrus → согласование.
2. Согласованный счёт руками вводится в банк-клиент, оплачивается.
3. Плательщик руками ставит статус «Оплачен» в задаче Pyrus.
4. Оплата автоматически попадает в ПланФакт — банк уже подключён к ПФ напрямую (подтверждено).
5. Сотрудник руками разносит операцию по салону и статье расхода в ПФ.

## Правки от пользователя (Чат 1, раунд 2)

1. Ручной ввод счёта в банк-клиент — тоже должен уйти, а не остаться как раньше планировалось.
2. Pyrus в этой схеме использовать не хотим — форму и согласование делаем своим модулем.

Уточнено у пользователя:
- Банк — **Модульбанк**. У него есть публичный API (`api.modulbank.ru`), токен генерируется прямо в личном кабинете без согласований. Есть метод `POST /v1/operation-upload/1c` — загружает платёжное поручение в формате 1С в личный кабинет со статусом «Черновик»; подписание — только внутри личного кабинета вручную.
- Уровень автоматизации отправки платежа: **платёжка готовится и загружается автоматически, отправляет (подписывает) человек**. Полная автоотправка через API отклонена — сознательно оставляем контроль над движением денег за человеком.

## Архитектура (to-be)

Новый модуль внутри существующего Flask-дашборда (`src/dashboard/`) — использует готовую авторизацию и роли (admin/manager/florist/florist_analyst), plus новый бэкенд-пакет `src/invoices/` (по аналогии с `src/moysklad/`).

**Собственная модель данных счёта** (SQLite/Postgres, как у `src/pyrus/storage.py`, но без Pyrus): номер счёта (уникальный, генерируется модулем), контрагент, реквизиты, сумма, назначение платежа, распределение по строкам (салон + статья расхода + сумма — может быть несколько строк на один счёт, см. уточнения ниже и `invoice_line_items`), файл/скан счёта, статус (черновик → на согласовании → согласован/отклонён → отправлен в банк → оплачен), история согласования, created_by.

### Пошагово

1. **Создание счёта** — сотрудник заполняет форму в дашборде. РУЧНОЙ. Модуль сам генерирует уникальный номер счёта и подставляет его в назначение платежа.
2. **Согласование** — workflow внутри модуля (один или несколько согласующих по ролям), уведомления (Telegram-бот). Решение — РУЧНОЕ, интерфейс и уведомления — автоматические.
3. **Формирование платёжки** — сразу после финального согласования модуль автоматически собирает платёжное поручение в формате 1С и отправляет через Модульбанк API (`POST /v1/operation-upload/1c`) → попадает в личный кабинет банка статусом «Черновик». Автоматически, без участия человека.
4. **Подпись и отправка** — ответственный заходит в личный кабинет Модульбанка, проверяет реквизиты и подписывает/отправляет платёж. РУЧНОЙ — единственная точка, где человек управляет движением денег.
5. **Поступление в ПланФакт** — банк уже настроен на автоматическую выгрузку в ПФ, операция появляется там сама, без нашего участия.
6. **Матчинг** — модуль опрашивает API ПланФакт на новые операции списания, вытаскивает номер счёта из назначения платежа (regex), ищет соответствующую запись в своей БД счетов.
7. **Авто-разноска** — при совпадении модуль через API ПФ проставляет статью расхода и салон (уже известны из карточки счёта) и помечает счёт в своей БД как «Оплачен». Автоматически.
8. **Исключения** — если матчинг не удался (нет номера, опечатка, один платёж на несколько счетов) — уведомление ответственному, ручная разноска как fallback. Не должно тихо теряться.

## Открытые вопросы для Чата 2 (разбивка на фазы)

- Точная спецификация метода `operation-upload/1c` Модульбанка: обязательные поля платёжки (БИК, счёт получателя, КБК/НДС и т.д.), формат номера счёта в назначении платежа, лимиты на частоту запросов.
- Точные эндпоинты API ПланФакт для (а) опроса новых операций — вебхук или поллинг, (б) обновления категории/объекта на операции. Нужен токен API ПФ, сейчас есть только косвенные данные (через агрегатор Albato).
- Схема согласования: сколько этапов, кто согласующие, нужны ли параллельные/последовательные согласования, лимиты по сумме (например, крупные счета — доп. согласующий).
- Формат уведомлений: Telegram-бот (уже есть канал у бизнеса) или встроенные уведомления в дашборде.
- Частичные оплаты / оплата нескольких счетов одним платёжным поручением — не пытаться угадывать, сразу в очередь на ручную разноску.
- Нужна ли миграция существующих незавершённых счетов из Pyrus, или стартуем модуль с нуля.

## Уточнения от владельца после исследования API ПланФакт (2026-08-15)

Прежде чем проектировать Фазу 6, разобрались, как ПланФакт реально устроен внутри (`apidoc.planfact.io`, `github.com/planfact/planfact-agent-kit`).

**Как разносится оплата в ПФ:** банковская операция — один верхнеуровневый объект, а разбивка по статьям/проектам живёт в дочернем массиве `operationParts[]` (пишется через `PUT /api/v1/operations/{id}` с `items[]`) — у каждой части своя статья (`operationCategoryId`), проект (`projectId`), сумма. Ограничений на число частей нет — это штатная фича самого ПФ («Разбить сумму» в интерфейсе), не то, что нужно изобретать нам.

**Вывод №1 — распределение живёт в нашей БД, не в платёжке.** Платёжка (формат 1С в Модульбанк) — чисто банковский документ, полей под статью/проект ПФ у неё нет и быть не может. Банк создаёт операцию в ПФ без какой-либо категоризации. Разноска — отдельный шаг: после матчинга операции со счётом модуль достаёт распределение из своей БД (решено сотрудником ещё на этапе создания счёта, до всякого банка) и пишет его в ПФ через API. Отсюда:

- Модель счёта меняется с «один `store_id` + один `expense_category_id` на счёт» на **множественную**: новая таблица `invoice_line_items` (invoice_id, store_id, expense_category_id, amount), сумма строк обязана равняться `invoices.amount`. Однострочный счёт — просто частный случай с одной строкой.
- Подтверждено владельцем: **бо́льшая часть счетов будет распределяться на несколько проектов/статей** — это основная модель, не редкое исключение. Проектировать Фазу 6 сразу под неё, не как доп. расширение потом.
- Форма создания счёта (Фаза 4, уже задеплоена) требует доработки: несколько строк распределения вместо одного салона/статьи.
- Фаза 6 при матчинге собирает `items[]` из строк `invoice_line_items` и пишет их одним вызовом `PUT /api/v1/operations/{id}` на найденную операцию.

**Вывод №2 — матчинг нужен по выделенному коду, не по «номеру счёта» как есть.** Сейчас `payment_purpose` — `"Оплата по счёту СЧ-000123, {описание}"` (`src/invoices/storage.py`, `create_invoice()`), и Фаза 6 планировала матчить регэкспом по этому номеру. Риск: `{описание}` — свободный текст сотрудника, может содержать номер счёта самого контрагента («по счёту №123 от 01.08») — регэксп на «номер счёта» рискует поймать не тот номер, либо потерять совпадение, если подписант в личном кабинете банка подправит назначение платежа перед отправкой.

Решение: отдельное поле **`match_code`** — короткий уникальный код в жёстко заданном формате, генерируется из id счёта (например `REF-000123`), не похожий на обычную нумерацию счетов контрагентов, вставляется в `payment_purpose` в фиксированном месте (в начале строки). Регэксп матчинга ищет строго `REF-\d{6}`, а не произвольный «номер счёта» — это исключает коллизии с любыми числами, которые сотрудник впишет в описание. `invoice_number` (СЧ-000123) остаётся человекочитаемым лейблом для бухгалтерии, но перестаёт быть ключом автоматического матчинга.

## Риски (pre-mortem)

- Ошибка в реквизитах при автосборке платёжки (напр. неверный БИК из карточки контрагента) → банк отклонит или платёж уйдёт не туда. Нужна валидация реквизитов на этапе создания счёта, не на этапе отправки.
- Токен Модульбанк API — чувствительные данные, хранить только в `.env` в корне проекта, не логировать.
- Изменение схемы согласования «на лету» (кто-то в отпуске, нужен временный согласующий) — учесть делегирование прав в workflow.
- Если у ПФ нет вебхуков, только поллинг → задержка между оплатой и разноской. Не критично, но заложить в ожидания.
- Ложное совпадение регэкспа при матчинге по «номеру счёта», если в свободном описании упомянут номер счёта самого контрагента → закрыто отдельным `match_code` в фиксированном формате (см. «Уточнения от владельца» выше).
- Отказ от Pyrus означает полный перенос процесса — нужен явный день/момент отсечки, чтобы не было счетов «зависших» в старой и новой системе одновременно.

---

## Фазы

Приоритет: сначала фазы, которые не зависят от внешних токенов (замена Pyrus своим модулем), — это уже само по себе снимает Pyrus из процесса и даёт немедленную пользу. Фазы 5–6 (банк, ПланФакт) стартуют, когда появятся токены.

### [x] Фаза 1 — Модель данных и миграции
- [x] `src/invoices/seed_data.py` — стартовые статьи расхода (плейсхолдер, потом смэппим на реальные категории ПФ)
- [x] `src/invoices/storage.py` — таблицы `invoices`, `invoice_expense_categories`; переиспользуем `stores` из `cashshifts.storage` (салон = точка продаж, нет смысла дублировать)
- [x] CRUD-функции: создание счёта, список с фильтрами, получение по ID, согласование/отклонение, CRUD категорий

### [x] Фаза 2 — Flask API
- [x] `src/invoices/server.py` — Blueprint `invoices_bp`, url_prefix `/api/invoices`
- [x] `POST /api/invoices` — создание счёта (статус сразу `on_approval`)
- [x] `GET /api/invoices`, `GET /api/invoices/<id>` — список и детали
- [x] `POST /api/invoices/<id>/approve`, `POST /api/invoices/<id>/reject` — только admin
- [x] `GET/POST/PUT/DELETE /api/invoices/categories` — CRUD статей расхода (admin)

### [x] Фаза 3 — Интеграция с авторизацией
- [x] `invoices` добавлен в `ALL_MODULES` (auth.py)
- [x] `ROLE_SECTIONS`: admin и manager получают доступ к созданию/просмотру; согласование — только admin (роль `manager` создаёт, не согласовывает сама себя)
- [x] Blueprint и инициализация таблиц зарегистрированы в `pyrus/server.py` (по образцу cashshifts), + роут страницы `/invoices` и статик `/invoices.js`

### [x] Фаза 4 — Frontend (замена формы Pyrus)
- [x] `src/dashboard/invoices.js` — список счетов, создание, согласование/отклонение (admin)
- [x] Пункт меню и секция страницы в `index.html`
- [x] Хуки навигации в `script.js` (заголовок раздела, активация модуля)

> После Фазы 4 сотрудники могут полностью перестать заводить счета в Pyrus — цикл «создание → согласование» полностью живёт в своём модуле. Отправка в банк и разноска в ПФ — пока вручную (как в старом процессе), автоматизируются в фазах 5–6.

### [x] Фаза 4.5 — Форма согласования, распределение по проектам/статьям, вложения, архив, справочники

Уточнения от владельца перед реализацией: поля формы согласования — Город, Планируемая дата оплаты, На кого выставлен счёт, ИНН/наименование/р.счёт/БИК контрагента, НДС, Сумма, Назначение платежа (обязательны только 5 отмеченных владельцем — Город, дата оплаты, плательщик, сумма, назначение платежа). Распределение по Проекту (=Салон в ПФ) и Статье — отдельно от этих полей, многострочное, опционально уже при создании, редактируется до архивации. Все справочники (Город, На кого выставлен счёт, НДС, Статьи расхода) редактируются только админом. Архивация — автоматическая по умолчанию, плюс ручной перевод в архив и обратно.

- [x] `src/invoices/storage.py`: три новых справочника (`invoice_cities`, `invoice_payers`, `invoice_vat_options`) с общим CRUD-хелпером (`_ref_*`), не дублирующим код 4 раза
- [x] Миграция схемы `invoices`: `store_id`/`expense_category_id` (NOT NULL) убраны из счёта, перенесены в `invoice_line_items` (многострочное распределение); добавлены `city_id`, `payer_id`, `vat_id`, `match_code`, `is_archived`, `archived_at`; `payment_purpose` стал обязательным прямым полем ввода (не автогенерируется из описания); существующие прод-счета мигрируют автоматически при старте (`_migrate_invoices_to_line_items`, проверено тестом на вручную собранной старой схеме)
- [x] `match_code` (`REF-000123`) — отдельный от `invoice_number` ключ для будущего матчинга в Фазе 6 (см. Фазу 6 ниже — эта работа перенесена сюда и сделана раньше срока)
- [x] `invoice_line_items` CRUD: `set_invoice_line_items()` с валидацией «сумма строк = сумма счёта», запрет правки для архивных счетов
- [x] `invoice_attachments`: загрузка файла на диск (`INVOICE_ATTACHMENTS_DIR`, whitelist расширений png/jpg/jpeg/webp/pdf, лимит 15 МБ), CRUD записи в БД
- [x] Авто-архивация: `reject_invoice()` архивирует сразу; `mark_invoice_paid()` + `set_invoice_line_items()` архивируют, как только счёт оплачен и полностью распределён (`is_invoice_fully_allocated`); плюс ручной `set_invoice_archived()` в обе стороны
- [x] Временная ручная кнопка «Отметить оплаченным» (`POST /<id>/mark-paid`, admin) — замена Фазы 5/6 до появления токенов банка/ПФ
- [x] `src/invoices/server.py`: эндпоинты для 3 новых справочников, `line-items`, `attachments` (upload/list/download/delete), `mark-paid`, `archive`/`unarchive`; фильтры списка расширены (контрагент, назначение платежа, диапазоны дат заведения/оплаты, салон — через `invoice_line_items`, автор, архив)
- [x] `src/dashboard/invoices.js` + `index.html`: новая форма создания (обязательные/опциональные поля по списку владельца), редактор строк распределения (добавить/удалить, live-сумма), загрузка вложений, панель фильтров, модалка деталей счёта (просмотр + правка распределения + вложения + действия по статусу), модалка управления 4 справочниками (admin)
- [x] `scripts/test_invoices_db.py` переписан под новую схему: создание с распределением, валидация суммы строк, оплата → авто-архив, ручной архив/разархив, фильтры, отдельный тест миграции старой схемы на вручную собранной БД — все проходят локально

### [ ] Фаза 5 — Автоформирование платёжки в Модульбанк
- [ ] Получить токен API Модульбанк (личный кабинет → Интеграция с банком)
- [ ] Сборка платёжного поручения в формате 1С из данных счёта (реквизиты контрагента — добавить поля в форму счёта)
- [ ] `POST /v1/operation-upload/1c` при переходе счёта в `approved` → статус `sent_to_bank`
- [ ] Обработка ошибок банка (503, невалидные реквизиты) — не терять счёт, показывать статус ошибки в UI

### [ ] Фаза 6 — Автоматическая разноска в ПланФакт
- [x] ~~Расширить модель счёта: таблица `invoice_line_items`~~ — сделано в Фазе 4.5 (перенесено раньше срока, т.к. понадобилось для формы согласования)
- [x] ~~Добавить поле `match_code`~~ — сделано в Фазе 4.5
- [x] ~~Доработать форму создания счёта — несколько строк распределения~~ — сделано в Фазе 4.5
- [ ] Получить токен API ПланФакт
- [ ] Поллинг новых операций списания (`POST /api/v1/operations/list`, или вебхук, если есть)
- [ ] Матчинг по `match_code` (не по `invoice_number`) в назначении платежа → запись в `invoices`
- [ ] Разноска через `PUT /api/v1/operations/{id}` с `items[]`, собранными из `invoice_line_items` (несколько статей/проектов на одну операцию)
- [ ] Обновление статуса счёта → `paid`
- [ ] Уведомление при неудачном матчинге (см. риски)

### [ ] Фаза 7 — Тестирование и отладка
- [ ] Сценарии: создание → согласование → отклонение → повторное создание
- [ ] Сквозной сценарий на реальном счёте (с токенами банка и ПФ)
- [ ] Частичная оплата / оплата нескольких счетов одним платежом — ручной fallback работает

---

## Итог

**Реализовано (Фазы 1–4):** свой модуль счетов на оплату (`src/invoices/`) — модель данных, Flask API, права доступа, фронтенд в дашборде. Задеплоено на прод, три раунда пост-деплойных фиксов после реального использования:

1. У существующих admin/manager не догрузилось право на новый модуль (`migrate_permissions_for_existing_users()` бэкафиллит роли только пользователям без единой записи в permissions) — добавлена `migrate_new_module_permissions()` в `auth.py`.
2. Модалка создания счёта затемняла всю страницу — оверлей был сиблингом `.modal`, а не потомком (во всех остальных модалках оверлей вложен внутрь `.modal` и наследует его `display:none`).
3. `src/dashboard/users.js` хранит свой отдельный список модулей (`MODULE_NAMES`, `ROLE_PERMISSIONS`) для формы управления пользователями, не синхронизированный с `auth.py` — `invoices` туда не попал, админ не мог выдать доступ вручную.

Все три — из-за того, что при добавлении нового модуля в этот дашборд нужно править несколько мест одновременно (чек-лист сохранён в памяти как `dashboard-new-module-checklist`). Проверено: `scripts/test_invoices_db.py` (создание → согласование → отклонение → фильтры) на throwaway SQLite; `py_compile`/`node --check` на все изменённые файлы; баланс тегов в `index.html`; живая проверка на проде после каждого фикса. Полный запуск сервера в браузере на этой машине не проверялся — нет рабочего venv с зависимостями проекта (см. память `project-no-local-dev-env`).

Сотрудники теперь могут полностью перестать заводить счета в Pyrus: создание и согласование живут в `/invoices`. Отправка в банк и разноска в ПФ — пока вручную, как в старом процессе.

**Не начато (Фазы 5–7):** автоматическое формирование платёжки в Модульбанк и авторазноска в ПланФакт — заблокированы отсутствием токенов API этих сервисов. Модель данных и форма уже готовы принять эту автоматизацию (см. Фазу 4.5) — Фаза 6 сведётся к подключению API ПФ поверх уже существующих `invoice_line_items`/`match_code`, без переделки схемы.

**Решение по ходу:** от идеи многошагового согласования отказались в пользу одного согласующего (admin) — соответствует размеру команды (владелец работает один), можно расширить позже без изменения схемы БД (поле `approved_by` уже отдельное).

**Фаза 4.5 задеплоена.** После деплоя — реальный сбойный прогон на проде (владелец пробовал завести счёт/добавить город), несколько раундов фиксов подряд:

1. Гонка двух gunicorn-воркеров (`amvera.yml`, `--workers 2`) при миграции `invoices` на новую схему — один воркер падал на "table already exists". Миграция теперь резюмируема и берёт эксклюзивную блокировку (`BEGIN IMMEDIATE`).
2. Мой же временный диагностический `@errorhandler(Exception)` перехватывал и штатные 401/403 (HTTPException — тоже Exception в Python) — убран, заменён точечными try/except внутри конкретных вьюх.
3. Настоящая причина исходного 500: "удалённые" (мягко, `is_active=0`) записи справочников блокируют повторное создание/переименование на то же имя через `UNIQUE(name)`, которое действует на всю таблицу, а не только на активные строки. Создание/переименование теперь реактивирует или подчищает "мёртвую" строку вместо падения.
4. "database is locked" — 2 воркера пишут в один файл SQLite дефолтным rollback-journal, где писатель блокирует всех читателей/писателей. Включён WAL-режим + увеличен `busy_timeout` (5с → 20с) во всех трёх модулях, шарящих файл (`auth.py`, `cashshifts/storage.py`, `invoices/storage.py`).

Подтверждено владельцем на проде: города/плательщики добавляются и переименовываются, счёт создаётся с распределением по проектам/статьям, согласование работает. Временная диагностика (`debug-schema`, подробный текст исключений в ответах) убрана после подтверждения.

**Урок:** SQLite + несколько gunicorn-воркеров на одном файле — источник неочевидных гонок при первом деплое схемных миграций; WAL + `busy_timeout` + резюмируемые миграции с эксклюзивной блокировкой — обязательный паттерн для любых будущих миграций в этом проекте, не только для invoices.
