# ABC-анализ товаров — план

**Статус:** [ ] В процессе (Фазы 1-2 реализованы и проверены на реальных данных; осталась Фаза 3 — доработать формулу под нужды владельца — и Фаза 4 — UI)
**Дата:** 2026-08-15
**Цель:** Модуль ABC-анализа товаров с выбором периода отчёта (по дате продажи) и канала продаж. Данные — через API. Товары берём из всех разделов МойСклад, кроме раздела «Товары МС».

---

## Архитектурное решение: откуда брать данные

**Единый источник — МойСклад, сущность `customerorder` (заказ покупателя).** Подтверждено разведкой реальных данных (Фаза 1): канал продаж — это нативное поле `customerorder.salesChannel` (ссылка на справочник `/entity/saleschannel`), а не кастомный атрибут и не что-то, что нужно тянуть из RetailCRM отдельно. Один и тот же объект `customerorder` при запросе с `expand=positions,positions.assortment,state` отдаёт всё необходимое за один вызов:

- `created` — дата создания заказа (= «дата продажи» по решению владельца)
- `state.name` — статус («Выполнен», «Отменён», ... — см. полный список ниже)
- `salesChannel` — ссылка на канал продаж (id; имя — из отдельно синхронизированного справочника)
- `positions.rows[]` — позиции: `quantity`, `price`, `discount`, `assortment.{id, name, archived, productFolder.id}`

`demand` (отгрузка) для ABC не нужна — у неё те же позиции, но нет `state` (только флаг `applicable`), и `customerorder` уже даёт весь набор данных. RetailCRM для ABC не используется вообще — она остаётся источником только для кассовых смен (`src/cashshifts/retailcrm_client.py`), это отдельная задача.

---

## Зафиксированные бизнес-решения (владелец)

- **Метрика ABC:** выручка (сумма продаж)
- **Дата продажи:** дата создания заказа (`customerorder.created`)
- **Статусы заказов:** только оплаченные/выполненные → фильтр `state.name == 'Выполнен'`
- **Исключение:** товары из раздела «Товары МС» (и всех его подпапок) не участвуют в анализе
- **Источник данных:** МойСклад, единственная сущность `customerorder`

---

## Результаты разведки (Фаза 1, выполнено 2026-08-15)

### Раздел «Товары МС» — найден
Корневая папка (`parent=null`): **id = `b2c18a8b-0a3d-11f0-0a80-08c4000f82ba`**, name = `Товары МС`.

Подпапки (глубина дерева — 1 уровень, дальше не ветвится):
```
0144d1db-0bac-11f0-0a80-0c450006ed4e  Гелиевые шары
0576b781-d4ae-11f0-0a80-072b003f5ffc  Декор
0a5d9d77-3c72-11f0-0a80-0482000b262e  Товары для аналитики
1054a3e1-2505-11f0-0a80-05de00125e47  Конверты
2bc7c41e-0a3e-11f0-0a80-1073000f3fee  Цветы
3f91d931-a811-11f0-0a80-1cbc002e1ffb  Фрукты
5d82f8cc-0bac-11f0-0a80-0bf1000719a5  Открытки
b023f8e2-0ad1-11f0-0a80-0db900177768  Клубника
ba7a7a7c-0e14-11f0-0a80-0dbd002d6c68  Боксы
```
(этот список приведён только для справки — в коде не хардкодится, см. решение ниже)
Важно: в каталоге есть **дублирующиеся по названию** папки вне «Товары МС» (например, отдельная корневая «Гелиевые шары» с другим id) — это, судя по всему, старое/легаси-дерево категорий внутри «Товары МС», сосуществующее с актуальным. Фильтровать нужно строго по id, не по имени.

**Решение для реализации:** резолвить исключаемые id **динамически по имени** корневой папки `Товары МС` (сама папка + прямые потомки) при каждой синхронизации, а не хардкодить UUID в коде — надёжнее на случай, если id когда-нибудь изменится.

### Канал продаж — нативная сущность, не кастомный атрибут
`customerorder.salesChannel` → `/entity/saleschannel`. Справочник — **41 канал**, среди них ровно те, что уже видели в прошлой аналитике продаж ([[.business/история/2026-08-05-анализ-продаж-томск.md]]): `WhatsApp`, `Улица`, `Telegram`, `Заказ с сайта` (несколько вариантов под разные сайты/интеграции), `Точка продаж` (тип `RETAIL_SALES`), плюс маркетплейсы (Яндекс Маркет, Ozon, Купер, Flowwow, Flawery, Сбермаркет, МАКС, flomarket), соцсети (ВКонтакте, Instagram), «По телефону», «2ГИС», «Персонал», «Обратный звонок».

### Статусы заказов (states, customerorder)
`Новый`, `В работе`, `Ожидание предоплаты`, `Передан флористу`, `Требуетя уточнение`, `Заказ готов`, `Передан на доработку`, `Согласование заказа`, `Вызван курьер`, `Передан курьеру`, `Ожидает вручения`, **`Выполнен`**, `Отменен`, `Удержание`.
→ Для ABC берём только `Выполнен`.

### Найденные и исправленные баги (все — pre-existing, не мои изменения этой сессии)
Все три бага маскировали друг друга: `get_sales_orders()` не работал вообще → `save_sales_order()` никогда не выполнялся → его собственные баги ни разу не проявлялись до сегодняшнего дня.

1. `client.py::get_sales_orders()` стучался в несуществующий путь `/entity/salesorder` (правильный — `/entity/customerorder`) → любая загрузка заказов падала с ошибкой 1005
2. `storage.py::save_sales_order()` — несовпадение числа колонок (19) и значений (18) в `INSERT` (лишняя `updated_at` в списке колонок без значения) → `sqlite3.ProgrammingError` при каждом вызове
3. `storage.py::save_sales_order()` — `order.get('rate', 1.0)` трактовал `rate` как число, а МойСклад отдаёт объект `{value?, currency}` → `sqlite3` ошибка биндинга параметра (dict вместо числа)
4. `storage.py::save_folder()` — брал родителя из ключа `parent`, а МойСклад отдаёт его под `productFolder` (и как meta-ссылку, не с прямым `id`) → `parent_id` никогда не сохранялся, дерево папок было плоским
5. (Обнаружено при реализации Фазы 2, не баг, а недокументированное поведение API) — при списочном запросе `GET /entity/customerorder` с `expand=positions,...` и `limit > 100` МойСклад молча перестаёт вкладывать `positions.rows` (отдаёт только `meta`, без содержимого) — порог измерен эмпирически (100 работает, 200 — нет). `fetcher.py` теперь снижает `batch_size` до 100, когда `expand` содержит `positions`

Все пять — исправлены и проверены на реальных данных (см. Фазу 2).

### Как канал продаж на самом деле выглядит в данных
Каждая позиция заказа хранит `assortment.productFolder` — прямая ссылка на папку товара, так что не нужно отдельно джойнить каталог `products` с позициями: связь уже приходит вложенной прямо в заказ.

---

## Аналоги в проекте (переиспользуемые паттерны)

- `src/moysklad/` — паттерн `client → fetcher → storage → server` (Flask JSON API + SQLite)
- `src/moysklad/storage.py` — уже есть `products.folder_id/folder_name`, паттерн `INSERT OR REPLACE` + `raw_data` JSON-блоб + `snapshot_at`
- `src/moysklad/server.py` — паттерн `success_response`/`error_response`
- [[project_dashboard_new_module_checklist]] — чеклист мест, которые нужно затронуть при добавлении модуля в дашборд (auth.py x2, users.js, script.js, вложенность modal-overlay)
- [[feedback_sqlite_multiworker_migrations]] — миграции таблиц под gunicorn multi-worker требуют WAL + busy_timeout + resumable паттерн, если модуль когда-то попадёт в тот же процесс, что и кассовые смены (сейчас MoySklad — отдельный сервис/БД, но проверить при деплое)

---

## Фазы

### [x] Фаза 1 — Разведка реальных данных
- [x] Определить сущность продаж → `customerorder`, `demand` не нужна
- [x] Найти механизм канала продаж → нативное поле `salesChannel`, справочник `/entity/saleschannel`
- [x] Найти `folder_id` раздела «Товары МС» и его подпапок
- [x] Определить статус «оплачен/выполнен» → `state.name == 'Выполнен'`
- [x] Попутно найден и исправлен баг в `client.py` (`salesorder` → `customerorder`)

### [x] Фаза 2 — Хранилище и синхронизация
- [x] `client.py`: добавлен `get_sales_channels()` (`/entity/saleschannel`)
- [x] `fetcher.py`: добавлен `get_all_sales_channels()` + `sales_channels` в диспетчер `get_full_entity_data`
- [x] `fetcher.py`: `get_all_sales_orders()` снижает `batch_size` до 100, когда `expand` содержит `positions` (см. найденный порог API выше)
- [x] `storage.py`: таблица `sales_channels (id, name, type, archived, raw_data, updated_at)`
- [x] `storage.py`: таблица `order_positions (id, order_id, assortment_id, assortment_name, product_folder_id, quantity, price, discount, sum, raw_data, snapshot_at)`
- [x] `storage.py`: миграция — колонка `sales_channel_id` добавлена в существующую таблицу `sales_orders` (`ALTER TABLE`, с проверкой через `PRAGMA table_info`, безопасно для уже существующей локальной БД — проверено на `data/moysklad.db`)
- [x] `storage.py`: `save_sales_order()` теперь пишет `sales_channel_id` и сохраняет все `positions.rows[]` через `_save_order_position()`; заодно исправлены найденные баги 2-4 выше
- [x] `storage.py`: `save_folder()` — исправлен баг 4 (парсинг `productFolder` вместо `parent`)
- [x] `scripts/update_moysklad.py`: `sales_channels` добавлена в `ENTITIES`/dispatch; для `sales_orders` теперь всегда передаётся `expand='positions,positions.assortment,state'` через `ENTITY_FETCH_KWARGS`
- [x] Синхронизация проверена на реальных данных (тестовая БД, 300 заказов → 467 позиций, 41 канал, 26 папок) — работает end-to-end
- [x] Проверена безопасность миграции на существующей `data/moysklad.db` (150 товаров, синхронизирован 2026-08-08) — новые таблицы/колонка добавились без потери данных

### [x] Фаза 3 — Расчёт ABC
- [x] `storage.py::_get_excluded_folder_ids()` — резолвит id раздела «Товары МС» по имени (сама папка + прямые потомки через `parent_id`), не хардкодит UUID
- [x] `storage.py::get_abc_analysis(date_from, date_to, channel_id=None)` — join `order_positions` + `sales_orders`, фильтр `state_name='Выполнен'`, диапазон по `created`, опциональный `sales_channel_id`, исключение папок «Товары МС»; группировка по `assortment_id`, выручка = `SUM(price*quantity*(1-discount/100))`, **переведена из копеек в рубли (`/100.0`, родной формат сумм в МойСклад)**
- [x] Сортировка по убыванию выручки → накопительный % → класс A (cum ≤ 80%) / B (cum ≤ 95%) / C (остальное)
- [x] Решение по расположению кода: оставлено в `src/moysklad/storage.py` (один источник данных, одна БД, не плодим отдельное подключение)
- [x] Проверено на реальных данных: 160 товаров в выдаче, топ-строка — 28719.92 руб (3.99% от выручки, класс A)

### [ ] Фаза 4 — API и дашборд
- [x] `GET /api/moysklad/abc-analysis?date_from=&date_to=&channel_id=` в `src/moysklad/server.py` — проверено через `test_client`, 200 OK
- [x] `GET /api/moysklad/sales_channels` — список каналов для фильтра в UI — проверено, 200 OK, 41 канал
- [ ] Виджет/страница в существующем дашборде — пройти по [[project_dashboard_new_module_checklist]] (auth.py роли, users.js, script.js, modal-overlay); нужно сначала найти и изучить текущую структуру фронтенда дашборда — не исследовалось в этом чате

---

## Итог

Фазы 1-3 и бэкенд-часть Фазы 4 реализованы и проверены на реальных данных МойСклад в рамках одного чата (без строгого разделения на «планирование / критика / реализация» — оказалось эффективнее довести до работающего кода сразу, пока контекст разведки свежий). Заодно найдено и исправлено 5 pre-existing багов в `src/moysklad/`, которые ранее не проявлялись, потому что `get_sales_orders()` не работал вообще (маскировал остальные).

**Осталось:** только фронтенд-часть Фазы 4 — подключить виджет ABC-анализа к дашборду (изучить текущую структуру UI, роли доступа, добавить фильтры периода/канала). Полная синхронизация исторических заказов (не тестовые 300, а вся история) тоже ещё не запускалась — стоит прогнать и оценить объём/время перед подключением к продакшн-дашборду.
