# Раздел «Задачи» на дашборде — план

**Статус:** [x] Завершён
**Дата:** 2026-08-15
**Цель:** Внутри существующей страницы Дашборд (`/dashboard`) — admin-only раздел «Задачи»:
бэклог идей/фич, которые планируются или в работе, с полем «на чём остановились»,
отмечаемых выполненными после реализации.

---

## Фазы

### [x] Фаза 1 — Backend-модуль `src/tasks/`
- [x] `storage.py`: таблица `dashboard_tasks`, CRUD, смена статуса, seed 4 стартовых задач
- [x] `server.py`: Blueprint `/api/tasks`, все роуты `role_required("admin")`

### [x] Фаза 2 — Регистрация в мастер-приложении
- [x] `src/pyrus/server.py`: регистрация blueprint, `init_tasks_tables()` при старте, статик-роут `/tasks.js`

### [x] Фаза 3 — Фронтенд
- [x] `src/dashboard/index.html`: секция «ЗАДАЧИ» на странице dashboard + модалка создания/правки
- [x] `src/dashboard/tasks.js`: список, создание/правка/смена статуса/удаление, admin-гейтинг через `userRoleChanged`
- [x] `src/dashboard/styles.css`: статус-бейджи `.status-idea`/`.status-in-progress`/`.status-done`, `.task-card`

### [x] Фаза 4 — Технический план
- [x] Этот файл

---

## Стартовые задачи (заведены в seed)

1. ABC-анализ товаров — в работе, осталось добавить переменные МойСклад в Амвера
2. Контроль остатков с уведомлениями управляющим — идея (`plans/2026-08-15-stock-monitoring.md`)
3. Форма списания для салонов — идея, через API МойСклад
4. Модуль согласования счетов — в работе (`plans/2026-08-14-invoice-approval-automation.md`)

---

## Итог

Реализовано полностью: backend-модуль, регистрация в мастер-приложении, фронтенд-секция
с CRUD и сменой статуса, 4 стартовые задачи. Доступ строго admin-only на каждом API-роуте
(строже, чем доступ к самой странице `/dashboard`, которая открыта и для manager) —
раздел скрывается на фронте через событие `userRoleChanged`.

Backend проверен через Flask test_client: admin получает 200 (список из 4 задач,
создание/смена статуса/удаление работают), manager получает 403 на всех роутах.
`init_tasks_tables()` идемпотентна — повторный вызов не дублирует seed.

Не проверено вручную в браузере (нет локального dev-окружения с браузером) — визуальную
проверку модалки/карточек и поведение для manager стоит сделать после деплоя.
